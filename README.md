# Hasura POC (Serverless)

## Getting started
1. Install Serverless Framework `npm install -g serverless`.
1. Run `yarn`.
1. Check one of the lambdas is working running `serverless invoke local -f <lambda>`.
